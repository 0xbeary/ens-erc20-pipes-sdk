# Substreams Configuration
# Copy this to .env.substreams and fill in your values

# Substreams API token (required)
SUBSTREAMS_TOKEN=your_substreams_token_here

# Substreams endpoint (optional, defaults to mainnet)
SUBSTREAMS_ENDPOINT=https://mainnet.eth.streamingfast.io

# Substreams package URL (optional)
SUBSTREAMS_SPKG=https://spkg.io/v1/files/ethereum-common-v0.3.1.spkg

# Output module name (optional)
SUBSTREAMS_MODULE=all_events

# Start block (optional, defaults to 23314199)
SUBSTREAMS_START_BLOCK=23314199

# Stop block (optional, leave empty for continuous streaming)
SUBSTREAMS_STOP_BLOCK=

# Indexer type: 'pipes' or 'substreams'
INDEXER_TYPE=substreams

# ClickHouse configuration (same as for pipes)
CLICKHOUSE_URL=http://localhost:8123
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=
CLICKHOUSE_DB=default
